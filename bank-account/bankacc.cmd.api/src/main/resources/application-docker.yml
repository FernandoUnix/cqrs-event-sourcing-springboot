server:
  port: 9091

axon:
  axonserver:
    servers: axon-server

spring:
  data:
    mongodb:
      host: mongo-container
      port: 27017
      database: bank

security:
  oauth2:
    resource:
      jwt:
        key-value: springbankSigningKey